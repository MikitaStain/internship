<?xml version = "1.0" encoding = "UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.6.xsd">

    <changeSet id="8" author="mikita">

        <comment>Add courses</comment>

        <sql>

            INSERT INTO application.courses(name)
            VALUES ('Java');

            INSERT INTO application.courses(name)
            VALUES ('Python');

            INSERT INTO application.courses(name)
            VALUES ('C++');

            INSERT INTO application.courses(name)
            VALUES ('Computer Since');

            INSERT INTO application.courses(name)
            VALUES ('Computer');

            INSERT INTO application.courses(name)
            VALUES ('JavaScript');

            INSERT INTO application.courses(name)
            VALUES ('React');

            INSERT INTO application.courses(name)
            VALUES ('Databases');

            INSERT INTO application.courses(name)
            VALUES ('PHP');

            INSERT INTO application.courses(name)
            VALUES ('Ruby');

        </sql>


    </changeSet>


</databaseChangeLog>