<?xml version = "1.0" encoding = "UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.6.xsd">

    <changeSet id="9" author="mikita">

        <comment>Add positions</comment>

        <sql>

            INSERT INTO application.positions(name)
            VALUES ('Computer systems manager');

            INSERT INTO application.positions(name)
            VALUES ('Network architect');

            INSERT INTO application.positions(name)
            VALUES ('Systems analyst');

            INSERT INTO application.positions(name)
            VALUES ('IT coordinator');

            INSERT INTO application.positions(name)
            VALUES ('Network administrator');

            INSERT INTO application.positions(name)
            VALUES ('Network engineer');

            INSERT INTO application.positions(name)
            VALUES ('Service desk analyst');

            INSERT INTO application.positions(name)
            VALUES ('System administrator');

            INSERT INTO application.positions(name)
            VALUES ('Wireless network engineer');

            INSERT INTO application.positions(name)
            VALUES ('Database administrator');


        </sql>


    </changeSet>


</databaseChangeLog>